# Native fullscreen requires window decorations: with `none`, `fullscreen` is
# silently ignored. The titlebar is hidden in fullscreen anyway, and native
# fullscreen keeps macOS tabs working, which non-native fullscreen does not.
window-decoration = auto
fullscreen = true

# This display wants a larger size than the Linux one.
font-size = 14
