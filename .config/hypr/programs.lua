-- Programs that the keybinds start.
-- Change a command here, and every bind that uses it changes too.

return {
    terminal     = "ghostty",
    file_manager = "dolphin",
    menu         = "rofi",
    browser      = "brave",
    lock         = "hyprlock",
}
